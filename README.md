# RLES 2v2

Simple web front end for RLES 2v2 — logo home page with sign-in.

## Run locally

```bash
npm install
npm run dev
```

Open the URL Vite prints (default `http://localhost:5173`).

## Build

```bash
npm run build
npm run preview
```
